{
  "name": "mcp-3d-printer-server",
  "private": false,
  "version": "0.0.0-security-hardening-pr8",
  "description": "Connects MCP to major 3D printer APIs with STL ops and slicing.",
  "type": "module",
  "scripts": {
    "start": "node ./dist/index.js",
    "build": "node --version >/dev/null && echo 'build step placeholder'",
    "audit": "npm audit --audit-level=high"
  },
  "dependencies": {
    "@babel/runtime": "^7.28.2",
    "axios": "^1.8.2"
  },
  "devDependencies": {},
  "overrides": {
    "semver": ">=7.5.2",
    "simple-update-notifier": ">=2.0.0",
    "brace-expansion": ">=1.1.12"
  },
  "engines": {
    "node": ">=18"
  }
}
